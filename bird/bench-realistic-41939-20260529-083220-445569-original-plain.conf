log "/dev/null" all;
router id 192.0.2.29;

filter f {
    accept;
}

protocol static test_routes {
    ipv4 { import filter f; };

    route 203.0.0.0/32 blackhole {
        bgp_path = prepend(bgp_path, 100);
        bgp_path = prepend(bgp_path, 101);
        bgp_path = prepend(bgp_path, 102);
        bgp_path = prepend(bgp_path, 103);
    };
    route 203.0.0.1/32 blackhole {
        bgp_path = prepend(bgp_path, 100);
        bgp_path = prepend(bgp_path, 101);
        bgp_path = prepend(bgp_path, 102);
        bgp_path = prepend(bgp_path, 103);
    };
    route 203.0.0.2/32 blackhole {
        bgp_path = prepend(bgp_path, 100);
        bgp_path = prepend(bgp_path, 101);
        bgp_path = prepend(bgp_path, 102);
        bgp_path = prepend(bgp_path, 103);
    };
    route 203.0.0.3/32 blackhole {
        bgp_path = prepend(bgp_path, 100);
        bgp_path = prepend(bgp_path, 101);
        bgp_path = prepend(bgp_path, 102);
        bgp_path = prepend(bgp_path, 103);
    };
    route 203.0.0.4/32 blackhole {
        bgp_path = prepend(bgp_path, 100);
        bgp_path = prepend(bgp_path, 101);
        bgp_path = prepend(bgp_path, 102);
        bgp_path = prepend(bgp_path, 103);
    };
    route 203.0.0.5/32 blackhole {
        bgp_path = prepend(bgp_path, 100);
        bgp_path = prepend(bgp_path, 101);
        bgp_path = prepend(bgp_path, 102);
        bgp_path = prepend(bgp_path, 103);
    };
    route 203.0.0.6/32 blackhole {
        bgp_path = prepend(bgp_path, 100);
        bgp_path = prepend(bgp_path, 101);
        bgp_path = prepend(bgp_path, 102);
        bgp_path = prepend(bgp_path, 103);
    };
    route 203.0.0.7/32 blackhole {
        bgp_path = prepend(bgp_path, 100);
        bgp_path = prepend(bgp_path, 101);
        bgp_path = prepend(bgp_path, 102);
        bgp_path = prepend(bgp_path, 103);
    };
    route 203.0.0.8/32 blackhole {
        bgp_path = prepend(bgp_path, 100);
        bgp_path = prepend(bgp_path, 101);
        bgp_path = prepend(bgp_path, 102);
        bgp_path = prepend(bgp_path, 103);
    };
    route 203.0.0.9/32 blackhole {
        bgp_path = prepend(bgp_path, 100);
        bgp_path = prepend(bgp_path, 101);
        bgp_path = prepend(bgp_path, 102);
        bgp_path = prepend(bgp_path, 103);
    };
}

protocol device {}
